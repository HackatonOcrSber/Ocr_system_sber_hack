import argparse

def parse_arg():
    parser = argparse.ArgumentParser()
    parser.add_argument("--path-to-image", type=str, default=None)
    parser.add_argument("--path-to-folder", type=str, default=None)

def main(arg):
    if

if __name__=="__main__":
    args = parse_arg()
    main(args)